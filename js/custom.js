(function($){
    const $win = $(window);
    const $doc = $(document);

    /*
    * Window Scroll
    */
    $win.scroll(function () {

    });

    /*
    * Window onLoad
    */
    $win.on('load', () => {

    });

    /*
    * Document Ready
    */
    $doc.ready(function(){
        
    });

})( jQuery );