(function($){
    /*
    * Javacript for front
    */
    const $win = $(window);
    const $doc = $(document);

    /*
    * Window Scroll
    */
    $win.on('scroll', function() {

    });

    /*
    * Window onLoad
    */
    $win.on('load', () => {

    });

    /*
    * Document Ready
    */
    $(function() {
        
    });

})( jQuery );
